# Changelog

## Unreleased

- add `hdf-core` with DMR/TMR reports, explicit repair, examples, and tests
- add `hdf-sync` serialized and critical-section wrapper paths
- add `hdf-layout` split placement metadata and complemented backend
- add `hdf-fault` corruption helpers, matrix tests, and host overhead report
- add `hdf-storage` persistent record scaffold and load/recover flow
- add `hdf-journal` compact event encoding and append-only journal buffer
- add `hdf-reference` end-to-end integration demo, tests, and measurement notes
- add top-level project docs, validation notes, and CI workflow
